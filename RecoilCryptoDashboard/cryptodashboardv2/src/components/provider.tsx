
export const Provider: React.FC<{providerName: string}> = ({providerName}) => {
    // Query get coins from provider
    // Get list of coinIds and amount
    // Pass coinIds and amount to coin components

    return (
        <div>{providerName}</div>
    );
}